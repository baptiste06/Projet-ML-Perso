task: object_detection
seed: 42
output_dir: outputs_pennfudan

model:
  weights: yolov8n.pt

data:
  yaml: data/data.pennfudan.yaml
  imgsz: 640
  batch: 8

train:
  epochs: 30
  lr0: 0.001
  weight_decay: 0.0005
  patience: 10

device: auto